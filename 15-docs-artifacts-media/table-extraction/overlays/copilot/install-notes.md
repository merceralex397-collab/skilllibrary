Install locations:

- repository: `.github/skills/table-extraction-codex-c/`
- user: `~/.config/github-copilot/skills/table-extraction-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Table Extraction should remain in `SKILL.md` and the references folder.
