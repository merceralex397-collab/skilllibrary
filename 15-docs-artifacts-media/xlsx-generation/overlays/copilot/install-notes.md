Install locations:

- repository: `.github/skills/xlsx-generation-codex-c/`
- user: `~/.config/github-copilot/skills/xlsx-generation-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for XLSX Generation should remain in `SKILL.md` and the references folder.
