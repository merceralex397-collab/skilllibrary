Install locations:

- repository: `.github/skills/image-heavy-pdfs-codex-c/`
- user: `~/.config/github-copilot/skills/image-heavy-pdfs-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Image Heavy Pdfs should remain in `SKILL.md` and the references folder.
