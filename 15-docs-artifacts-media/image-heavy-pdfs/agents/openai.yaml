interface:
  display_name: "Image-Heavy PDFs"
  short_description: "Produce image-heavy pdfs artifacts with source fidelity, audi..."
  default_prompt: "Use $image-heavy-pdfs-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
