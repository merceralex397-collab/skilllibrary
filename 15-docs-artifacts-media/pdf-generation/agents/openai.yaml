interface:
  display_name: "PDF Generation"
  short_description: "Produce pdf generation artifacts with source fidelity, audien..."
  default_prompt: "Use $pdf-generation-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
