Install locations:

- repository: `.github/skills/pdf-generation-codex-c/`
- user: `~/.config/github-copilot/skills/pdf-generation-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for PDF Generation should remain in `SKILL.md` and the references folder.
