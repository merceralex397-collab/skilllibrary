interface:
  display_name: "Research Synthesis"
  short_description: "Produce research synthesis artifacts with source fidelity, au..."
  default_prompt: "Use $research-synthesis-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
