Install locations:

- repository: `.github/skills/pdf-extraction-codex-c/`
- user: `~/.config/github-copilot/skills/pdf-extraction-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for PDF Extraction should remain in `SKILL.md` and the references folder.
