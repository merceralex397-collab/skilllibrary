Install locations:

- repository: `.github/skills/pdf-editor-codex-c/`
- user: `~/.config/github-copilot/skills/pdf-editor-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for PDF Editor should remain in `SKILL.md` and the references folder.
