# Artifact Structure

DOCX Generation should produce artifacts that are easy to scan and revise.

- Lead with the decision or main point.
- Group evidence and supporting detail behind clear headings.
- Keep source fidelity visible.
- Show open questions instead of smoothing them away.

Related skills worth checking nearby: `internal-comms`, `pptx-generation`, `pdf-generation`, `xlsx-generation`.
