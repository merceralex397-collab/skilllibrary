Install locations:

- repository: `.github/skills/docx-generation-codex-c/`
- user: `~/.config/github-copilot/skills/docx-generation-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for DOCX Generation should remain in `SKILL.md` and the references folder.
