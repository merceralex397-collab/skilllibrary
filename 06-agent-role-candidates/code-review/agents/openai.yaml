interface:
  display_name: "Code Review"
  short_description: "Execute the code review role with clear scope, evidence, and..."
  default_prompt: "Use $code-review-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
