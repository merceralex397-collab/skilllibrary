Install locations:

- repository: `.github/skills/planner-codex-c/`
- user: `~/.config/github-copilot/skills/planner-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Planner should remain in `SKILL.md` and the references folder.
