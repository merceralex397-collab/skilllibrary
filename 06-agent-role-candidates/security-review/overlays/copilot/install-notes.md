Install locations:

- repository: `.github/skills/security-review-codex-c/`
- user: `~/.config/github-copilot/skills/security-review-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Security Review should remain in `SKILL.md` and the references folder.
