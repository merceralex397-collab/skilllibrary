Install locations:

- repository: `.github/skills/qa-validation-codex-c/`
- user: `~/.config/github-copilot/skills/qa-validation-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for QA Validation should remain in `SKILL.md` and the references folder.
