Install locations:

- repository: `.github/skills/context-summarization-codex-c/`
- user: `~/.config/github-copilot/skills/context-summarization-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Context Summarization should remain in `SKILL.md` and the references folder.
