Install locations:

- repository: `.github/skills/docs-handoff-codex-c/`
- user: `~/.config/github-copilot/skills/docs-handoff-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Docs Handoff should remain in `SKILL.md` and the references folder.
