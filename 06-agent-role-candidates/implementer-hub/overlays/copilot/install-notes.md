Install locations:

- repository: `.github/skills/implementer-hub-codex-c/`
- user: `~/.config/github-copilot/skills/implementer-hub-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Implementer Hub should remain in `SKILL.md` and the references folder.
