Install locations:

- repository: `.github/skills/shell-inspection-codex-c/`
- user: `~/.config/github-copilot/skills/shell-inspection-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Shell Inspection should remain in `SKILL.md` and the references folder.
