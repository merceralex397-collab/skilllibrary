Install locations:

- repository: `.github/skills/backlog-verifier-codex-c/`
- user: `~/.config/github-copilot/skills/backlog-verifier-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Backlog Verifier should remain in `SKILL.md` and the references folder.
