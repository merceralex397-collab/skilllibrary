Install locations:

- repository: `.github/skills/team-leader-codex-c/`
- user: `~/.config/github-copilot/skills/team-leader-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Team Leader should remain in `SKILL.md` and the references folder.
