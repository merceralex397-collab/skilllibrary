Install locations:

- repository: `.github/skills/ticket-audit-codex-c/`
- user: `~/.config/github-copilot/skills/ticket-audit-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Ticket Audit should remain in `SKILL.md` and the references folder.
