Install locations:

- repository: `.github/skills/implementer-context-codex-c/`
- user: `~/.config/github-copilot/skills/implementer-context-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Implementer Context should remain in `SKILL.md` and the references folder.
