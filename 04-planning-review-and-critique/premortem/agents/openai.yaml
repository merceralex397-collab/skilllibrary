interface:
  display_name: "Premortem"
  short_description: "Review premortem work with explicit evidence, risk ranking, a..."
  default_prompt: "Use $premortem-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
