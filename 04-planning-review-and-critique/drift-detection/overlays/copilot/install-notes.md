Install locations:

- repository: `.github/skills/drift-detection-codex-c/`
- user: `~/.config/github-copilot/skills/drift-detection-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Drift Detection should remain in `SKILL.md` and the references folder.
