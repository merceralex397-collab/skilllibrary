Install locations:

- repository: `.github/skills/docker-containers-codex-c/`
- user: `~/.config/github-copilot/skills/docker-containers-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Docker Containers should remain in `SKILL.md` and the references folder.
