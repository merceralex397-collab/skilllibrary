interface:
  display_name: "Cloudflare Worker Patterns"
  short_description: "Handle cloudflare worker patterns changes with preflight chec..."
  default_prompt: "Use $cloudflare-worker-patterns-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
