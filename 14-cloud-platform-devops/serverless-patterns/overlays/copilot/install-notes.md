Install locations:

- repository: `.github/skills/serverless-patterns-codex-c/`
- user: `~/.config/github-copilot/skills/serverless-patterns-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Serverless Patterns should remain in `SKILL.md` and the references folder.
