Install locations:

- repository: `.github/skills/vercel-codex-c/`
- user: `~/.config/github-copilot/skills/vercel-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Vercel should remain in `SKILL.md` and the references folder.
