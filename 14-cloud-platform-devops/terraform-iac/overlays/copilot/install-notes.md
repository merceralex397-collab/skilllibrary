Install locations:

- repository: `.github/skills/terraform-iac-codex-c/`
- user: `~/.config/github-copilot/skills/terraform-iac-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Terraform IaC should remain in `SKILL.md` and the references folder.
