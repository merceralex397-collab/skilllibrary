Install locations:

- repository: `.github/skills/cost-monitoring-codex-c/`
- user: `~/.config/github-copilot/skills/cost-monitoring-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Cost Monitoring should remain in `SKILL.md` and the references folder.
