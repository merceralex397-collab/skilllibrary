Install locations:

- repository: `.github/skills/cloudflare-codex-c/`
- user: `~/.config/github-copilot/skills/cloudflare-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Cloudflare should remain in `SKILL.md` and the references folder.
