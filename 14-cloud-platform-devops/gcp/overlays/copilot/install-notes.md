Install locations:

- repository: `.github/skills/gcp-codex-c/`
- user: `~/.config/github-copilot/skills/gcp-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for GCP should remain in `SKILL.md` and the references folder.
