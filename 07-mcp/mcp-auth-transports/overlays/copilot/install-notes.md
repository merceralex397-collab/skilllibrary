Install locations:

- repository: `.github/skills/mcp-auth-transports-codex-c/`
- user: `~/.config/github-copilot/skills/mcp-auth-transports-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for MCP Auth Transports should remain in `SKILL.md` and the references folder.
