Install locations:

- repository: `.github/skills/mcp-inspector-debugging-codex-c/`
- user: `~/.config/github-copilot/skills/mcp-inspector-debugging-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for MCP Inspector Debugging should remain in `SKILL.md` and the references folder.
