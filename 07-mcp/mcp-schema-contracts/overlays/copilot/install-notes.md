Install locations:

- repository: `.github/skills/mcp-schema-contracts-codex-c/`
- user: `~/.config/github-copilot/skills/mcp-schema-contracts-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for MCP Schema Contracts should remain in `SKILL.md` and the references folder.
