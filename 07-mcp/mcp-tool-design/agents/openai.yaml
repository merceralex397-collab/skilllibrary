interface:
  display_name: "MCP Tool Design"
  short_description: "Design and verify mcp tool design with explicit contracts, pe..."
  default_prompt: "Use $mcp-tool-design-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
