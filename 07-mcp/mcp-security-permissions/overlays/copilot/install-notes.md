Install locations:

- repository: `.github/skills/mcp-security-permissions-codex-c/`
- user: `~/.config/github-copilot/skills/mcp-security-permissions-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for MCP Security Permissions should remain in `SKILL.md` and the references folder.
