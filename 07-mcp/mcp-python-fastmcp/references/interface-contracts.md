# Interface Contracts

Use MCP Python FastMCP to make the interface explicit.

- Name the host and target transport.
- Specify tool/resource/prompt shapes.
- Define input and output schemas.
- List permission or auth assumptions.

Related skills worth checking nearby: `mcp-security-permissions`, `mcp-host-integration`, `mcp-typescript-sdk`, `mcp-go-server`.
