Install locations:

- repository: `.github/skills/mcp-development-codex-c/`
- user: `~/.config/github-copilot/skills/mcp-development-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for MCP Development should remain in `SKILL.md` and the references folder.
