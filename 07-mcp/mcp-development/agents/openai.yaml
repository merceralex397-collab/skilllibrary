interface:
  display_name: "MCP Development"
  short_description: "Design and verify mcp development with explicit contracts, pe..."
  default_prompt: "Use $mcp-development-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
