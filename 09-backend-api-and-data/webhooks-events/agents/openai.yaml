interface:
  display_name: "Webhooks and Events"
  short_description: "Design safe webhook receipt, delivery, and replay handling."
  default_prompt: "Use $webhooks-events-codex-c to review this webhook receiver for signature verification, acknowledgement timing, duplicate delivery safety, and replay support."
policy:
  allow_implicit_invocation: true
