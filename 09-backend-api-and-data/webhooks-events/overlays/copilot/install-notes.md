Install locations:

- repository: `.github/skills/webhooks-events-codex-c/`
- user: `~/.config/github-copilot/skills/webhooks-events-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Webhooks Events should remain in `SKILL.md` and the references folder.
