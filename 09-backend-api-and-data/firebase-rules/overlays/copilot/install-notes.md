Install locations:

- repository: `.github/skills/firebase-rules-codex-c/`
- user: `~/.config/github-copilot/skills/firebase-rules-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Firebase Rules should remain in `SKILL.md` and the references folder.
