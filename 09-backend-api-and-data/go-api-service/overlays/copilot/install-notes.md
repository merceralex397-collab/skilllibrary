Install locations:

- repository: `.github/skills/go-api-service-codex-c/`
- user: `~/.config/github-copilot/skills/go-api-service-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Go API Service should remain in `SKILL.md` and the references folder.
