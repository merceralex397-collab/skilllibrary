interface:
  display_name: "SQLite"
  short_description: "Apply sqlite patterns with clear contracts, persistence rules..."
  default_prompt: "Use $sqlite-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
