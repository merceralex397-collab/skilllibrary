Install locations:

- repository: `.github/skills/sqlite-codex-c/`
- user: `~/.config/github-copilot/skills/sqlite-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for SQLite should remain in `SKILL.md` and the references folder.
