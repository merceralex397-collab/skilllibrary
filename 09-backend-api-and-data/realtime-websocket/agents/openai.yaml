interface:
  display_name: "Realtime and WebSocket"
  short_description: "Apply realtime and websocket patterns with clear contracts, p..."
  default_prompt: "Use $realtime-websocket-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
