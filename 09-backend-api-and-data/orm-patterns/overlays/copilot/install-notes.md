Install locations:

- repository: `.github/skills/orm-patterns-codex-c/`
- user: `~/.config/github-copilot/skills/orm-patterns-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for ORM Patterns should remain in `SKILL.md` and the references folder.
