Install locations:

- repository: `.github/skills/api-debugging-codex-c/`
- user: `~/.config/github-copilot/skills/api-debugging-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for API Debugging should remain in `SKILL.md` and the references folder.
