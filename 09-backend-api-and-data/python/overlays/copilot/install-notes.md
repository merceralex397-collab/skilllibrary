Install locations:

- repository: `.github/skills/python-codex-c/`
- user: `~/.config/github-copilot/skills/python-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Python should remain in `SKILL.md` and the references folder.
