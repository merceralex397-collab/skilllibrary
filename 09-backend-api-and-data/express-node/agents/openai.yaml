interface:
  display_name: "Express and Node"
  short_description: "Apply express and node patterns with clear contracts, persist..."
  default_prompt: "Use $express-node-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
