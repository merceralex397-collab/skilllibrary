Install locations:

- repository: `.github/skills/rate-limits-retries-codex-c/`
- user: `~/.config/github-copilot/skills/rate-limits-retries-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Rate Limits Retries should remain in `SKILL.md` and the references folder.
