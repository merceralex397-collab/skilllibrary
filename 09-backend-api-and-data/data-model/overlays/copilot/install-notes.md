Install locations:

- repository: `.github/skills/data-model-codex-c/`
- user: `~/.config/github-copilot/skills/data-model-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Data Model should remain in `SKILL.md` and the references folder.
