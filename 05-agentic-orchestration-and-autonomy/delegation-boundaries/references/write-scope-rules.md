# Write Scope Rules

- one implementer per write surface unless a merge owner is defined
- explorers and reviewers should usually stay read-only
- if two tasks both touch the same file, redesign the split first
