Install locations:

- repository: `.github/skills/delegation-boundaries-codex-c/`
- user: `~/.config/github-copilot/skills/delegation-boundaries-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Delegation Boundaries should remain in `SKILL.md` and the references folder.
