Install locations:

- repository: `.github/skills/workflow-state-memory-codex-c/`
- user: `~/.config/github-copilot/skills/workflow-state-memory-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Workflow State Memory should remain in `SKILL.md` and the references folder.
