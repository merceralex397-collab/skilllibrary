Install locations:

- repository: `.github/skills/parallel-lane-safety-codex-c/`
- user: `~/.config/github-copilot/skills/parallel-lane-safety-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Parallel Lane Safety should remain in `SKILL.md` and the references folder.
