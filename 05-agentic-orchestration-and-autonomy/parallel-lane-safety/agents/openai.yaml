interface:
  display_name: "Parallel Lane Safety"
  short_description: "Coordinate parallel lane safety work with bounded delegation,..."
  default_prompt: "Use $parallel-lane-safety-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
