Install locations:

- repository: `.github/skills/artifact-contracts-codex-c/`
- user: `~/.config/github-copilot/skills/artifact-contracts-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Artifact Contracts should remain in `SKILL.md` and the references folder.
