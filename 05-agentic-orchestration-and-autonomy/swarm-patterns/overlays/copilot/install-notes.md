Install locations:

- repository: `.github/skills/swarm-patterns-codex-c/`
- user: `~/.config/github-copilot/skills/swarm-patterns-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Swarm Patterns should remain in `SKILL.md` and the references folder.
