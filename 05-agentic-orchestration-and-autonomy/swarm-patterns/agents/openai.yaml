interface:
  display_name: "Swarm Patterns"
  short_description: "Coordinate swarm patterns work with bounded delegation, artif..."
  default_prompt: "Use $swarm-patterns-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
