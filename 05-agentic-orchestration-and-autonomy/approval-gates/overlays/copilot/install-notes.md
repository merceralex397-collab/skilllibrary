Install locations:

- repository: `.github/skills/approval-gates-codex-c/`
- user: `~/.config/github-copilot/skills/approval-gates-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Approval Gates should remain in `SKILL.md` and the references folder.
