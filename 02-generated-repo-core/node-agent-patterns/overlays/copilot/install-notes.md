Install locations:

- repository: `.github/skills/node-agent-patterns-codex-c/`
- user: `~/.config/github-copilot/skills/node-agent-patterns-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Node Agent Patterns should remain in `SKILL.md` and the references folder.
