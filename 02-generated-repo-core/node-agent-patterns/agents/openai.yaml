interface:
  display_name: "Node Agent Patterns"
  short_description: "Apply node agent patterns through a concrete workflow with ex..."
  default_prompt: "Use $node-agent-patterns-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
