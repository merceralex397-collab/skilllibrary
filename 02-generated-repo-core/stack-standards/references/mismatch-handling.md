# Mismatch Handling

When docs and code disagree:

- prefer enforced CI and config over prose
- mention the mismatch explicitly
- avoid silently following a stale README
- if the repo is migrating, note both the current enforced path and the intended target
