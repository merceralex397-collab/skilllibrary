# Evidence Sources

Strong stack signals:

- `package.json`, `pnpm-lock.yaml`, `yarn.lock`
- `pyproject.toml`, `requirements.txt`, `uv.lock`
- `Cargo.toml`, `go.mod`, `.csproj`
- CI workflows
- linter, formatter, and test configs
- existing source trees and test directories

Weak signals:

- directory names alone
- stale README claims
- archive docs
