interface:
  display_name: "Auth Patterns"
  short_description: "Apply auth patterns through a concrete workflow with explicit..."
  default_prompt: "Use $auth-patterns-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
