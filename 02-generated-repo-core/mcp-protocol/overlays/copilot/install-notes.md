Install locations:

- repository: `.github/skills/mcp-protocol-codex-c/`
- user: `~/.config/github-copilot/skills/mcp-protocol-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for MCP Protocol should remain in `SKILL.md` and the references folder.
