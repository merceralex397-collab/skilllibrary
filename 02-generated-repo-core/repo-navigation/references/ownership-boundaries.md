# Ownership Boundaries

When a repo is partitioned by user, agent, or product area:

- stay inside the assigned surface unless instructed otherwise
- do not rewrite siblings just to make the layout prettier
- treat peer folders as separate ownership domains
