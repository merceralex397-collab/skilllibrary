Install locations:

- repository: `.github/skills/external-api-client-codex-c/`
- user: `~/.config/github-copilot/skills/external-api-client-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for External API Client should remain in `SKILL.md` and the references folder.
