interface:
  display_name: "Planning"
  short_description: "Apply planning through a concrete workflow with explicit checks"
  default_prompt: "Use $planning-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
