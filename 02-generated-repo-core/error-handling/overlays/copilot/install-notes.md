Install locations:

- repository: `.github/skills/error-handling-codex-c/`
- user: `~/.config/github-copilot/skills/error-handling-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Error Handling should remain in `SKILL.md` and the references folder.
