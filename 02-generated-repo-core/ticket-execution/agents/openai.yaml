interface:
  display_name: "Ticket Execution"
  short_description: "Apply ticket execution through a concrete workflow with expli..."
  default_prompt: "Use $ticket-execution-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
