interface:
  display_name: "Security Hardening"
  short_description: "Apply security hardening through a concrete workflow with exp..."
  default_prompt: "Use $security-hardening-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
