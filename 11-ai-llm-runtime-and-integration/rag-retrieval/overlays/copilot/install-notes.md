Install locations:

- repository: `.github/skills/rag-retrieval-codex-c/`
- user: `~/.config/github-copilot/skills/rag-retrieval-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for RAG Retrieval should remain in `SKILL.md` and the references folder.
