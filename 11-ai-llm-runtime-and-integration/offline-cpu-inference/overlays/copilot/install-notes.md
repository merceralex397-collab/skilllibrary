Install locations:

- repository: `.github/skills/offline-cpu-inference-codex-c/`
- user: `~/.config/github-copilot/skills/offline-cpu-inference-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Offline CPU Inference should remain in `SKILL.md` and the references folder.
