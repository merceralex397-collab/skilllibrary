Install locations:

- repository: `.github/skills/model-routing-codex-c/`
- user: `~/.config/github-copilot/skills/model-routing-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Model Routing should remain in `SKILL.md` and the references folder.
