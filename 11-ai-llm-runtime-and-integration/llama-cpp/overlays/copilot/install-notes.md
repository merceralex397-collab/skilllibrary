Install locations:

- repository: `.github/skills/llama-cpp-codex-c/`
- user: `~/.config/github-copilot/skills/llama-cpp-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Llama Cpp should remain in `SKILL.md` and the references folder.
