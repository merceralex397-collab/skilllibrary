Install locations:

- repository: `.github/skills/embeddings-indexing-codex-c/`
- user: `~/.config/github-copilot/skills/embeddings-indexing-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Embeddings Indexing should remain in `SKILL.md` and the references folder.
