Install locations:

- repository: `.github/skills/context-management-memory-codex-c/`
- user: `~/.config/github-copilot/skills/context-management-memory-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Context Management Memory should remain in `SKILL.md` and the references folder.
