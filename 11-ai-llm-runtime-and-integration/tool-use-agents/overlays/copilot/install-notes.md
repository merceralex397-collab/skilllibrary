Install locations:

- repository: `.github/skills/tool-use-agents-codex-c/`
- user: `~/.config/github-copilot/skills/tool-use-agents-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Tool Use Agents should remain in `SKILL.md` and the references folder.
