interface:
  display_name: "Model Architecture"
  short_description: "Apply model architecture with runtime boundaries, eval cases,..."
  default_prompt: "Use $model-architecture-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
