Install locations:

- repository: `.github/skills/eval-dataset-design-codex-c/`
- user: `~/.config/github-copilot/skills/eval-dataset-design-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Eval Dataset Design should remain in `SKILL.md` and the references folder.
