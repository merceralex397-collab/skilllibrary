Install locations:

- repository: `.github/skills/distillation-compression-codex-c/`
- user: `~/.config/github-copilot/skills/distillation-compression-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Distillation Compression should remain in `SKILL.md` and the references folder.
