Install locations:

- repository: `.github/skills/model-merging-codex-c/`
- user: `~/.config/github-copilot/skills/model-merging-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Model Merging should remain in `SKILL.md` and the references folder.
