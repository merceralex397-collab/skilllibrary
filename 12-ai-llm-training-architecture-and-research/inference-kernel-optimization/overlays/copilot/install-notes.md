Install locations:

- repository: `.github/skills/inference-kernel-optimization-codex-c/`
- user: `~/.config/github-copilot/skills/inference-kernel-optimization-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Inference Kernel Optimization should remain in `SKILL.md` and the references folder.
