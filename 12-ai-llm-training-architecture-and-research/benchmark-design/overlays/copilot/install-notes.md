Install locations:

- repository: `.github/skills/benchmark-design-codex-c/`
- user: `~/.config/github-copilot/skills/benchmark-design-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Benchmark Design should remain in `SKILL.md` and the references folder.
