interface:
  display_name: "MoE Architecture"
  short_description: "Apply moe architecture with runtime boundaries, eval cases, a..."
  default_prompt: "Use $moe-architecture-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
