Install locations:

- repository: `.github/skills/pretraining-pipeline-codex-c/`
- user: `~/.config/github-copilot/skills/pretraining-pipeline-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Pretraining Pipeline should remain in `SKILL.md` and the references folder.
