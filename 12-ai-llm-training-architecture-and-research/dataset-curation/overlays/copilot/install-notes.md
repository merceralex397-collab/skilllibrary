Install locations:

- repository: `.github/skills/dataset-curation-codex-c/`
- user: `~/.config/github-copilot/skills/dataset-curation-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Dataset Curation should remain in `SKILL.md` and the references folder.
