Install locations:

- repository: `.github/skills/training-infrastructure-codex-c/`
- user: `~/.config/github-copilot/skills/training-infrastructure-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Training Infrastructure should remain in `SKILL.md` and the references folder.
