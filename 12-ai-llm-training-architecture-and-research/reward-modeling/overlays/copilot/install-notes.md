Install locations:

- repository: `.github/skills/reward-modeling-codex-c/`
- user: `~/.config/github-copilot/skills/reward-modeling-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Reward Modeling should remain in `SKILL.md` and the references folder.
