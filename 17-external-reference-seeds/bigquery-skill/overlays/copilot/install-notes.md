Install locations:

- repository: `.github/skills/bigquery-skill-codex-c/`
- user: `~/.config/github-copilot/skills/bigquery-skill-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for BigQuery Skill should remain in `SKILL.md` and the references folder.
