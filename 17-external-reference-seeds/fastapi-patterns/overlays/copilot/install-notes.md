Install locations:

- repository: `.github/skills/fastapi-patterns-codex-c/`
- user: `~/.config/github-copilot/skills/fastapi-patterns-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for FastAPI Patterns should remain in `SKILL.md` and the references folder.
