Install locations:

- repository: `.github/skills/misc-helper-codex-c/`
- user: `~/.config/github-copilot/skills/misc-helper-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Misc Helper should remain in `SKILL.md` and the references folder.
