name: misc-helper-codex-c
activation:
  requires_user_approval: true
  description: "Allows the agent to load the Misc Helper skill and its bundled references."
permissions:
  read_paths:
    - ./SKILL.md
    - ./references/**
    - ./evals/**
  execute_paths:
    - ./scripts/**
