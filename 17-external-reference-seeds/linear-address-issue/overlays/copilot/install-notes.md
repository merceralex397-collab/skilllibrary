Install locations:

- repository: `.github/skills/linear-address-issue-codex-c/`
- user: `~/.config/github-copilot/skills/linear-address-issue-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Linear Address Issue should remain in `SKILL.md` and the references folder.
