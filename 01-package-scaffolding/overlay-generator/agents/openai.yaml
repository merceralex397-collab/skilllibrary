interface:
  display_name: "Overlay Generator"
  short_description: "Build overlay generator artifacts with deterministic structur..."
  default_prompt: "Use $overlay-generator-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
