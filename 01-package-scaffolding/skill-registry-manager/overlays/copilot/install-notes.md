Install locations:

- repository: `.github/skills/skill-registry-manager-codex-c/`
- user: `~/.config/github-copilot/skills/skill-registry-manager-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Skill Registry Manager should remain in `SKILL.md` and the references folder.
