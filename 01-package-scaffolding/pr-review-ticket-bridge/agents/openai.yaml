interface:
  display_name: "PR Review Ticket Bridge"
  short_description: "Review pr review ticket bridge work with explicit evidence, r..."
  default_prompt: "Use $pr-review-ticket-bridge-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
