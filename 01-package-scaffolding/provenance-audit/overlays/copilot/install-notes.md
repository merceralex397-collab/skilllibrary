Install locations:

- repository: `.github/skills/provenance-audit-codex-c/`
- user: `~/.config/github-copilot/skills/provenance-audit-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Provenance Audit should remain in `SKILL.md` and the references folder.
