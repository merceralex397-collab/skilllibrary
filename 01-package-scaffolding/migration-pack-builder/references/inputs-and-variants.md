# Inputs And Variants

Before using Migration Pack Builder, normalize the real inputs.

- Task boundary and required artifact set.
- Naming and packaging rules.
- Compatibility constraints.
- Whether the output is reusable or one-off.

Related skills worth checking nearby: `community-skill-harvester`, `stack-profile-detector`, `skill-deprecation-manager`, `provenance-audit`.
