Install locations:

- repository: `.github/skills/domain-scouting-codex-c/`
- user: `~/.config/github-copilot/skills/domain-scouting-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Domain Scouting should remain in `SKILL.md` and the references folder.
