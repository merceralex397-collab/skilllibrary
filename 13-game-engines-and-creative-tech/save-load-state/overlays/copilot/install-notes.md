Install locations:

- repository: `.github/skills/save-load-state-codex-c/`
- user: `~/.config/github-copilot/skills/save-load-state-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Save Load State should remain in `SKILL.md` and the references folder.
