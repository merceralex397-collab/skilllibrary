interface:
  display_name: "Save and Load State"
  short_description: "Use save and load state to guide creative system decisions wi..."
  default_prompt: "Use $save-load-state-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
