Install locations:

- repository: `.github/skills/unreal-engine-codex-c/`
- user: `~/.config/github-copilot/skills/unreal-engine-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Unreal Engine should remain in `SKILL.md` and the references folder.
