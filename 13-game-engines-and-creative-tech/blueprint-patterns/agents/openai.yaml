interface:
  display_name: "Blueprint Patterns"
  short_description: "Use blueprint patterns to guide creative system decisions wit..."
  default_prompt: "Use $blueprint-patterns-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
