Install locations:

- repository: `.github/skills/skill-variant-splitting-codex-c/`
- user: `~/.config/github-copilot/skills/skill-variant-splitting-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Skill Variant Splitting should remain in `SKILL.md` and the references folder.
