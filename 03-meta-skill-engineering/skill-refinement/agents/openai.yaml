interface:
  display_name: "Skill Refinement"
  short_description: "Build skill refinement artifacts with deterministic structure..."
  default_prompt: "Use $skill-refinement-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
