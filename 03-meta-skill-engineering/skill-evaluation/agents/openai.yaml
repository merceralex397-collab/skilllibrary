interface:
  display_name: "Skill Evaluation"
  short_description: "Build skill evaluation artifacts with deterministic structure..."
  default_prompt: "Use $skill-evaluation-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
