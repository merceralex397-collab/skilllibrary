interface:
  display_name: "Skill Variant Splitting"
  short_description: "Build skill variant splitting artifacts with deterministic st..."
  default_prompt: "Use $skill-variant-splitting-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
