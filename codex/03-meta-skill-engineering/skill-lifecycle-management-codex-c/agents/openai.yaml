interface:
  display_name: "Skill Lifecycle Management"
  short_description: "Build skill lifecycle management artifacts with deterministic..."
  default_prompt: "Use $skill-lifecycle-management-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
