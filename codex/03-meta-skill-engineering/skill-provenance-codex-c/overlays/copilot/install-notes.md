Install locations:

- repository: `.github/skills/skill-provenance-codex-c/`
- user: `~/.config/github-copilot/skills/skill-provenance-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Skill Provenance should remain in `SKILL.md` and the references folder.
