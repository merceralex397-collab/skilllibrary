interface:
  display_name: "Skill Provenance"
  short_description: "Build skill provenance artifacts with deterministic structure..."
  default_prompt: "Use $skill-provenance-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
