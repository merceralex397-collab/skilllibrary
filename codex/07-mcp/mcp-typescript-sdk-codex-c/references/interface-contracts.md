# Interface Contracts

Use MCP TypeScript SDK to make the interface explicit.

- Name the host and target transport.
- Specify tool/resource/prompt shapes.
- Define input and output schemas.
- List permission or auth assumptions.

Related skills worth checking nearby: `mcp-host-integration`, `mcp-python-fastmcp`, `mcp-go-server`, `mcp-resources-prompts`.
