Install locations:

- repository: `.github/skills/mcp-typescript-sdk-codex-c/`
- user: `~/.config/github-copilot/skills/mcp-typescript-sdk-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for MCP TypeScript SDK should remain in `SKILL.md` and the references folder.
