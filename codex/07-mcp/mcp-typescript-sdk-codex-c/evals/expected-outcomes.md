# Expected Outcomes

A good MCP TypeScript SDK run should:

- trigger only when the task genuinely matches the skill boundary,
- produce a response that includes `Runtime Surface`, `Contracts`, `Risk Controls`, `Validation Plan`,
- stay within the mcp workflow instead of drifting into generic advice,
- call out uncertainty and next validation when the evidence is incomplete,
- avoid fabricating implementation details or sources.
