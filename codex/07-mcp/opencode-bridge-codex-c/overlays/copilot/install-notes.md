Install locations:

- repository: `.github/skills/opencode-bridge-codex-c/`
- user: `~/.config/github-copilot/skills/opencode-bridge-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for OpenCode Bridge should remain in `SKILL.md` and the references folder.
