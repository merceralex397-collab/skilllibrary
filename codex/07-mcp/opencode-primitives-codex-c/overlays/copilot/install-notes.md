Install locations:

- repository: `.github/skills/opencode-primitives-codex-c/`
- user: `~/.config/github-copilot/skills/opencode-primitives-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for OpenCode Primitives should remain in `SKILL.md` and the references folder.
