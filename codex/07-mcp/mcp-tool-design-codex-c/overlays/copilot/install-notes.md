Install locations:

- repository: `.github/skills/mcp-tool-design-codex-c/`
- user: `~/.config/github-copilot/skills/mcp-tool-design-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for MCP Tool Design should remain in `SKILL.md` and the references folder.
