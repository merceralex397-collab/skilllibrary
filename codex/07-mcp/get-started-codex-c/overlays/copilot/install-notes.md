Install locations:

- repository: `.github/skills/get-started-codex-c/`
- user: `~/.config/github-copilot/skills/get-started-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Get Started should remain in `SKILL.md` and the references folder.
