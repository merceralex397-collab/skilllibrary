Install locations:

- repository: `.github/skills/mcp-go-server-codex-c/`
- user: `~/.config/github-copilot/skills/mcp-go-server-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for MCP Go Server should remain in `SKILL.md` and the references folder.
