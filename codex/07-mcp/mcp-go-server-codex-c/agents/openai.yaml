interface:
  display_name: "MCP Go Server"
  short_description: "Design and verify mcp go server with explicit contracts, perm..."
  default_prompt: "Use $mcp-go-server-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
