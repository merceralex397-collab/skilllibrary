Install locations:

- repository: `.github/skills/bash-codex-c/`
- user: `~/.config/github-copilot/skills/bash-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Bash should remain in `SKILL.md` and the references folder.
