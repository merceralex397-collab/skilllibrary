# Preflight Checklist

Before using SSH and tmux Remote Workflow, confirm:

- environment and target stage,
- credentials or secrets handling,
- rollback path,
- verification command or signal,
- operator ownership.

Related skills worth checking nearby: `cross-platform-shell`, `linux-ubuntu-ops`, `systemd-services`, `release-binaries`.
