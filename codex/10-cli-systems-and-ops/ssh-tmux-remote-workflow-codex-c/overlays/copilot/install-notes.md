Install locations:

- repository: `.github/skills/ssh-tmux-remote-workflow-codex-c/`
- user: `~/.config/github-copilot/skills/ssh-tmux-remote-workflow-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for SSH tmux Remote Workflow should remain in `SKILL.md` and the references folder.
