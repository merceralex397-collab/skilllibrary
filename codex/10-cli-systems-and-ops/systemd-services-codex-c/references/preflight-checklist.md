# Preflight Checklist

Before using systemd Services, confirm:

- environment and target stage,
- credentials or secrets handling,
- rollback path,
- verification command or signal,
- operator ownership.

Related skills worth checking nearby: `linux-ubuntu-ops`, `ssh-tmux-remote-workflow`, `release-binaries`, `proxmox-shell-scripting`.
