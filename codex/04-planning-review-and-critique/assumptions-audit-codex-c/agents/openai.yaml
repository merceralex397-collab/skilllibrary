interface:
  display_name: "Assumptions Audit"
  short_description: "Review assumptions audit work with explicit evidence, risk ra..."
  default_prompt: "Use $assumptions-audit-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
