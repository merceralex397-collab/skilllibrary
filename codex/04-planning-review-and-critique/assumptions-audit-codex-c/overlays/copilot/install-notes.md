Install locations:

- repository: `.github/skills/assumptions-audit-codex-c/`
- user: `~/.config/github-copilot/skills/assumptions-audit-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Assumptions Audit should remain in `SKILL.md` and the references folder.
