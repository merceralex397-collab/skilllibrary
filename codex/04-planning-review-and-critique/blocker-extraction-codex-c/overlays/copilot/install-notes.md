Install locations:

- repository: `.github/skills/blocker-extraction-codex-c/`
- user: `~/.config/github-copilot/skills/blocker-extraction-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Blocker Extraction should remain in `SKILL.md` and the references folder.
