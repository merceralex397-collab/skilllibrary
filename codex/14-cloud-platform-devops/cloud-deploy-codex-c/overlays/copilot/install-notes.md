Install locations:

- repository: `.github/skills/cloud-deploy-codex-c/`
- user: `~/.config/github-copilot/skills/cloud-deploy-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Cloud Deploy should remain in `SKILL.md` and the references folder.
