interface:
  display_name: "Cloud Deploy"
  short_description: "Handle cloud deploy changes with preflight checks, verificati..."
  default_prompt: "Use $cloud-deploy-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
