Install locations:

- repository: `.github/skills/secret-management-codex-c/`
- user: `~/.config/github-copilot/skills/secret-management-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Secret Management should remain in `SKILL.md` and the references folder.
