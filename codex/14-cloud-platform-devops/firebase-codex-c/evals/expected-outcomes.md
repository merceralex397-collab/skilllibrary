# Expected Outcomes

A good Firebase run should:

- trigger only when the task genuinely matches the skill boundary,
- produce a response that includes `Environment`, `Change Plan`, `Verification`, `Rollback or Monitoring`,
- stay within the ops workflow instead of drifting into generic advice,
- call out uncertainty and next validation when the evidence is incomplete,
- avoid fabricating implementation details or sources.
