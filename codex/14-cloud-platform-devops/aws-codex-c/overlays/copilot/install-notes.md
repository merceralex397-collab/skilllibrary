Install locations:

- repository: `.github/skills/aws-codex-c/`
- user: `~/.config/github-copilot/skills/aws-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for AWS should remain in `SKILL.md` and the references folder.
