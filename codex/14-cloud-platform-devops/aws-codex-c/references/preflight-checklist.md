# Preflight Checklist

Before using AWS, confirm:

- environment and target stage,
- credentials or secrets handling,
- rollback path,
- verification command or signal,
- operator ownership.

Related skills worth checking nearby: `firebase`, `gcp`, `vercel`, `docker-containers`.
