Install locations:

- repository: `.github/skills/cloudflare-worker-patterns-codex-c/`
- user: `~/.config/github-copilot/skills/cloudflare-worker-patterns-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Cloudflare Worker Patterns should remain in `SKILL.md` and the references folder.
