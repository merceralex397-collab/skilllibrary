interface:
  display_name: "Serverless Patterns"
  short_description: "Handle serverless patterns changes with preflight checks, ver..."
  default_prompt: "Use $serverless-patterns-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
