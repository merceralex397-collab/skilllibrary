Install locations:

- repository: `.github/skills/queues-cron-workers-codex-c/`
- user: `~/.config/github-copilot/skills/queues-cron-workers-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Queues Cron Workers should remain in `SKILL.md` and the references folder.
