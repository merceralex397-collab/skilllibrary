Install locations:

- repository: `.github/skills/cargo-lock-manager-codex-c/`
- user: `~/.config/github-copilot/skills/cargo-lock-manager-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Cargo Lock Manager should remain in `SKILL.md` and the references folder.
