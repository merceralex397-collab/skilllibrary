interface:
  display_name: "FastAPI Patterns"
  short_description: "Run fastapi patterns with grounded sources, comparison, and d..."
  default_prompt: "Use $fastapi-patterns-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
