Install locations:

- repository: `.github/skills/image-prompt-direction-codex-c/`
- user: `~/.config/github-copilot/skills/image-prompt-direction-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Image Prompt Direction should remain in `SKILL.md` and the references folder.
