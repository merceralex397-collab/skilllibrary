Install locations:

- repository: `.github/skills/bigquery-codex-c/`
- user: `~/.config/github-copilot/skills/bigquery-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for BigQuery should remain in `SKILL.md` and the references folder.
