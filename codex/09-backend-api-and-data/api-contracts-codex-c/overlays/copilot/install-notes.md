Install locations:

- repository: `.github/skills/api-contracts-codex-c/`
- user: `~/.config/github-copilot/skills/api-contracts-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for API Contracts should remain in `SKILL.md` and the references folder.
