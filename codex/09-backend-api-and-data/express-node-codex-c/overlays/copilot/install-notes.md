Install locations:

- repository: `.github/skills/express-node-codex-c/`
- user: `~/.config/github-copilot/skills/express-node-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Express Node should remain in `SKILL.md` and the references folder.
