interface:
  display_name: "Observability and Logging"
  short_description: "Design backend telemetry that is searchable, correlated, and safe."
  default_prompt: "Use $observability-logging-codex-c to review this service's logs, metrics, traces, and correlation fields for production usefulness and redaction safety."
policy:
  allow_implicit_invocation: true
