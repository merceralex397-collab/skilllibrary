Install locations:

- repository: `.github/skills/background-jobs-queues-codex-c/`
- user: `~/.config/github-copilot/skills/background-jobs-queues-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Background Jobs Queues should remain in `SKILL.md` and the references folder.
