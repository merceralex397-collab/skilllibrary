# Implementation Patterns

Use these backend checks while applying Realtime and WebSocket.

- Make contracts explicit at the boundary.
- Define validation and failure behavior together.
- Keep persistence rules and operational assumptions visible.
- Prefer idempotent or reversible flows where possible.

Related skills worth checking nearby: `rate-limits-retries`, `webhooks-events`, `api-debugging`, `api-contracts`.
