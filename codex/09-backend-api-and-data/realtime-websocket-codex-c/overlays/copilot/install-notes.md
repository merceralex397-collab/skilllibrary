Install locations:

- repository: `.github/skills/realtime-websocket-codex-c/`
- user: `~/.config/github-copilot/skills/realtime-websocket-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Realtime WebSocket should remain in `SKILL.md` and the references folder.
