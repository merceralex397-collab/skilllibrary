# Expected Outcomes

A good Python run should:

- trigger only when the task genuinely matches the skill boundary,
- produce a response that includes `System Surface`, `Chosen Pattern`, `Edge Cases`, `Validation`,
- stay within the backend workflow instead of drifting into generic advice,
- call out uncertainty and next validation when the evidence is incomplete,
- avoid fabricating implementation details or sources.
