name: ticket-creator-codex-c
activation:
  requires_user_approval: true
  description: "Allows the agent to load the Ticket Creator skill and its bundled references."
permissions:
  read_paths:
    - ./SKILL.md
    - ./references/**
    - ./evals/**
  execute_paths:
    - ./scripts/**
