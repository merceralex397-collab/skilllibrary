Install locations:

- repository: `.github/skills/code-review-codex-c/`
- user: `~/.config/github-copilot/skills/code-review-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Code Review should remain in `SKILL.md` and the references folder.
