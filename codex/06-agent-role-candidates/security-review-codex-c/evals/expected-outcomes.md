# Expected Outcomes

A good Security Review run should:

- trigger only when the task genuinely matches the skill boundary,
- produce a response that includes `Assigned Scope`, `Evidence Collected`, `Recommended Action`, `Handoff Notes`,
- stay within the role workflow instead of drifting into generic advice,
- call out uncertainty and next validation when the evidence is incomplete,
- avoid fabricating implementation details or sources.
