interface:
  display_name: "RAG and Retrieval"
  short_description: "Apply rag and retrieval with runtime boundaries, eval cases,..."
  default_prompt: "Use $rag-retrieval-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
