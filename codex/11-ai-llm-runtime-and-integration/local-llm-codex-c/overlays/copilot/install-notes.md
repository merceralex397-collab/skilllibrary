Install locations:

- repository: `.github/skills/local-llm-codex-c/`
- user: `~/.config/github-copilot/skills/local-llm-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Local LLM should remain in `SKILL.md` and the references folder.
