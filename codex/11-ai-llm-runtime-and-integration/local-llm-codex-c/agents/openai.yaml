interface:
  display_name: "Local LLM"
  short_description: "Apply local llm with runtime boundaries, eval cases, and safe..."
  default_prompt: "Use $local-llm-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
