Install locations:

- repository: `.github/skills/vllm-serving-codex-c/`
- user: `~/.config/github-copilot/skills/vllm-serving-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for vLLM Serving should remain in `SKILL.md` and the references folder.
