Install locations:

- repository: `.github/skills/model-selection-codex-c/`
- user: `~/.config/github-copilot/skills/model-selection-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Model Selection should remain in `SKILL.md` and the references folder.
