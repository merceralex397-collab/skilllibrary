Install locations:

- repository: `.github/skills/inference-serving-codex-c/`
- user: `~/.config/github-copilot/skills/inference-serving-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Inference Serving should remain in `SKILL.md` and the references folder.
