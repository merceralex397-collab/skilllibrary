Install locations:

- repository: `.github/skills/vue-codex-c/`
- user: `~/.config/github-copilot/skills/vue-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Vue should remain in `SKILL.md` and the references folder.
