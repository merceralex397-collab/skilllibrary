interface:
  display_name: "UX Design"
  short_description: "Apply ux design patterns with explicit UI state, accessibilit..."
  default_prompt: "Use $ux-design-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
