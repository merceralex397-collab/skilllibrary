Install locations:

- repository: `.github/skills/state-management-codex-c/`
- user: `~/.config/github-copilot/skills/state-management-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for State Management should remain in `SKILL.md` and the references folder.
