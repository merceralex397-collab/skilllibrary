Install locations:

- repository: `.github/skills/design-tokens-codex-c/`
- user: `~/.config/github-copilot/skills/design-tokens-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Design Tokens should remain in `SKILL.md` and the references folder.
