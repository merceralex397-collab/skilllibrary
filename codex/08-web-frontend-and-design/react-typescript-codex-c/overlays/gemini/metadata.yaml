name: react-typescript-codex-c
activation:
  requires_user_approval: true
  description: "Allows the agent to load the React TypeScript skill and its bundled references."
permissions:
  read_paths:
    - ./SKILL.md
    - ./references/**
    - ./evals/**
  execute_paths:
    - ./scripts/**
