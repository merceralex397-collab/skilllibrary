Install locations:

- repository: `.github/skills/tailwind-shadcn-codex-c/`
- user: `~/.config/github-copilot/skills/tailwind-shadcn-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Tailwind Shadcn should remain in `SKILL.md` and the references folder.
