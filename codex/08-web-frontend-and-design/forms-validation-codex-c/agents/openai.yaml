interface:
  display_name: "Forms and Validation"
  short_description: "Apply forms and validation patterns with explicit UI state, a..."
  default_prompt: "Use $forms-validation-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
