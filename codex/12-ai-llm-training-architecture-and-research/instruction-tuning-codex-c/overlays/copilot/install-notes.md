Install locations:

- repository: `.github/skills/instruction-tuning-codex-c/`
- user: `~/.config/github-copilot/skills/instruction-tuning-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Instruction Tuning should remain in `SKILL.md` and the references folder.
