Install locations:

- repository: `.github/skills/fine-tuning-codex-c/`
- user: `~/.config/github-copilot/skills/fine-tuning-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Fine Tuning should remain in `SKILL.md` and the references folder.
