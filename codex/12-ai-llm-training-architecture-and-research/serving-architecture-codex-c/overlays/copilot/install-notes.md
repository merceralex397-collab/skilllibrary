Install locations:

- repository: `.github/skills/serving-architecture-codex-c/`
- user: `~/.config/github-copilot/skills/serving-architecture-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Serving Architecture should remain in `SKILL.md` and the references folder.
