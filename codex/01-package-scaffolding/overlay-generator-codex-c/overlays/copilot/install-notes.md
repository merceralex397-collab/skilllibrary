Install locations:

- repository: `.github/skills/overlay-generator-codex-c/`
- user: `~/.config/github-copilot/skills/overlay-generator-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Overlay Generator should remain in `SKILL.md` and the references folder.
