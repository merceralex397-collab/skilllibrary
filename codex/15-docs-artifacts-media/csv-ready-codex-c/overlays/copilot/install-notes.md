Install locations:

- repository: `.github/skills/csv-ready-codex-c/`
- user: `~/.config/github-copilot/skills/csv-ready-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for CSV Ready should remain in `SKILL.md` and the references folder.
