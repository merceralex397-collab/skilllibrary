Install locations:

- repository: `.github/skills/image-editor-codex-c/`
- user: `~/.config/github-copilot/skills/image-editor-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Image Editor should remain in `SKILL.md` and the references folder.
