interface:
  display_name: "Spec Authoring"
  short_description: "Produce spec authoring artifacts with source fidelity, audien..."
  default_prompt: "Use $spec-authoring-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
