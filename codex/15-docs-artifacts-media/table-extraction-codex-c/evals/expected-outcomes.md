# Expected Outcomes

A good Table Extraction run should:

- trigger only when the task genuinely matches the skill boundary,
- produce a response that includes `Audience`, `Artifact Structure`, `Draft or Final Output`, `Review Notes`,
- stay within the docs workflow instead of drifting into generic advice,
- call out uncertainty and next validation when the evidence is incomplete,
- avoid fabricating implementation details or sources.
