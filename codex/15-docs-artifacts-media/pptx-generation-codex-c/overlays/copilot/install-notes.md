Install locations:

- repository: `.github/skills/pptx-generation-codex-c/`
- user: `~/.config/github-copilot/skills/pptx-generation-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for PPTX Generation should remain in `SKILL.md` and the references folder.
