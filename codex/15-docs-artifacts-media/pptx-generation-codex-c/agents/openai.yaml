interface:
  display_name: "PowerPoint Generation"
  short_description: "Produce powerpoint generation artifacts with source fidelity,..."
  default_prompt: "Use $pptx-generation-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
