interface:
  display_name: "Document Writing"
  short_description: "Produce document writing artifacts with source fidelity, audi..."
  default_prompt: "Use $document-writing-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
