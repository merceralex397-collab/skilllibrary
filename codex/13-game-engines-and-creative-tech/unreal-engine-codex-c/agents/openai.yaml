interface:
  display_name: "Unreal Engine"
  short_description: "Use unreal engine to guide creative system decisions without..."
  default_prompt: "Use $unreal-engine-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
