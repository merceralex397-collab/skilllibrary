# Expected Outcomes

A good Asset Pipeline run should:

- trigger only when the task genuinely matches the skill boundary,
- produce a response that includes `Creative Goal`, `Implementation Plan`, `Iteration Notes`, `Validation`,
- stay within the creative workflow instead of drifting into generic advice,
- call out uncertainty and next validation when the evidence is incomplete,
- avoid fabricating implementation details or sources.
