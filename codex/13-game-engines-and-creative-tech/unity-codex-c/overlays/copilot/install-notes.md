Install locations:

- repository: `.github/skills/unity-codex-c/`
- user: `~/.config/github-copilot/skills/unity-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Unity should remain in `SKILL.md` and the references folder.
