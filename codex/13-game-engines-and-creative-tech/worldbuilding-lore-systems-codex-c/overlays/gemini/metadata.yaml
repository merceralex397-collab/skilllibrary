name: worldbuilding-lore-systems-codex-c
activation:
  requires_user_approval: true
  description: "Allows the agent to load the Worldbuilding Lore Systems skill and its bundled references."
permissions:
  read_paths:
    - ./SKILL.md
    - ./references/**
    - ./evals/**
  execute_paths:
    - ./scripts/**
