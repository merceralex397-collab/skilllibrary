Install locations:

- repository: `.github/skills/input-mapping-controller-codex-c/`
- user: `~/.config/github-copilot/skills/input-mapping-controller-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Input Mapping Controller should remain in `SKILL.md` and the references folder.
