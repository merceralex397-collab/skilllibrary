interface:
  display_name: "Multiplayer Netcode"
  short_description: "Use multiplayer netcode to guide creative system decisions wi..."
  default_prompt: "Use $multiplayer-netcode-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
