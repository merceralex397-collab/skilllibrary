Install locations:

- repository: `.github/skills/multiplayer-netcode-codex-c/`
- user: `~/.config/github-copilot/skills/multiplayer-netcode-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Multiplayer Netcode should remain in `SKILL.md` and the references folder.
