Install locations:

- repository: `.github/skills/manager-hierarchy-design-codex-c/`
- user: `~/.config/github-copilot/skills/manager-hierarchy-design-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Manager Hierarchy Design should remain in `SKILL.md` and the references folder.
