Install locations:

- repository: `.github/skills/process-versioning-codex-c/`
- user: `~/.config/github-copilot/skills/process-versioning-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Process Versioning should remain in `SKILL.md` and the references folder.
