interface:
  display_name: "Workflow State Memory"
  short_description: "Coordinate workflow state memory work with bounded delegation..."
  default_prompt: "Use $workflow-state-memory-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
