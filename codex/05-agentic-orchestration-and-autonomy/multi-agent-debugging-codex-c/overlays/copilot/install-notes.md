Install locations:

- repository: `.github/skills/multi-agent-debugging-codex-c/`
- user: `~/.config/github-copilot/skills/multi-agent-debugging-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Multi Agent Debugging should remain in `SKILL.md` and the references folder.
