# Local vs Delegate Tests

Keep local if:

- the answer is needed before the next step
- the task is tightly coupled to current reasoning
- the scope is too fuzzy to specify cleanly

Delegate if:

- the subtask is concrete and bounded
- the output can be consumed without redoing it
- the work is independent of the current blocker
