Install locations:

- repository: `.github/skills/goal-decomposition-codex-c/`
- user: `~/.config/github-copilot/skills/goal-decomposition-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Goal Decomposition should remain in `SKILL.md` and the references folder.
