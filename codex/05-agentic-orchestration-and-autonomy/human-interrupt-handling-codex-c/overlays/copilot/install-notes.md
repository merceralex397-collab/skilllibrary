Install locations:

- repository: `.github/skills/human-interrupt-handling-codex-c/`
- user: `~/.config/github-copilot/skills/human-interrupt-handling-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Human Interrupt Handling should remain in `SKILL.md` and the references folder.
