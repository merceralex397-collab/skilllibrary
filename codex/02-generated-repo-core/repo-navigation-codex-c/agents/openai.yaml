interface:
  display_name: "Repo Navigation"
  short_description: "Apply repo navigation through a concrete workflow with explic..."
  default_prompt: "Use $repo-navigation-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
