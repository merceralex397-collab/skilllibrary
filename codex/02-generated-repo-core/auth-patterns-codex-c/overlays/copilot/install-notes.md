Install locations:

- repository: `.github/skills/auth-patterns-codex-c/`
- user: `~/.config/github-copilot/skills/auth-patterns-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Auth Patterns should remain in `SKILL.md` and the references folder.
