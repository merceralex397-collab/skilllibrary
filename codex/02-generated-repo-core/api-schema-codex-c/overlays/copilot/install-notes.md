Install locations:

- repository: `.github/skills/api-schema-codex-c/`
- user: `~/.config/github-copilot/skills/api-schema-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for API Schema should remain in `SKILL.md` and the references folder.
