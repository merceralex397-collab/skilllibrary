interface:
  display_name: "Stack Standards"
  short_description: "Apply stack standards through a concrete workflow with explic..."
  default_prompt: "Use $stack-standards-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
