Install locations:

- repository: `.github/skills/database-persistence-codex-c/`
- user: `~/.config/github-copilot/skills/database-persistence-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Database Persistence should remain in `SKILL.md` and the references folder.
