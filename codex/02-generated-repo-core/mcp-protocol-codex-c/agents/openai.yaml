interface:
  display_name: "MCP Protocol"
  short_description: "Apply mcp protocol through a concrete workflow with explicit..."
  default_prompt: "Use $mcp-protocol-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
