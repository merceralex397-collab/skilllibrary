interface:
  display_name: "Project Context"
  short_description: "Apply project context through a concrete workflow with explic..."
  default_prompt: "Use $project-context-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
