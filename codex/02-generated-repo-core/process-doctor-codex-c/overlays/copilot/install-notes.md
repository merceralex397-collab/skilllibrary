Install locations:

- repository: `.github/skills/process-doctor-codex-c/`
- user: `~/.config/github-copilot/skills/process-doctor-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Process Doctor should remain in `SKILL.md` and the references folder.
