# Expected Outcomes

A good Node Agent Patterns run should:

- trigger only when the task genuinely matches the skill boundary,
- produce a response that includes `Scope`, `Approach`, `Checks`, `Next Step`,
- stay within the generic workflow instead of drifting into generic advice,
- call out uncertainty and next validation when the evidence is incomplete,
- avoid fabricating implementation details or sources.
