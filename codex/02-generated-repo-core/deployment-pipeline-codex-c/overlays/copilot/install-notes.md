Install locations:

- repository: `.github/skills/deployment-pipeline-codex-c/`
- user: `~/.config/github-copilot/skills/deployment-pipeline-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Deployment Pipeline should remain in `SKILL.md` and the references folder.
