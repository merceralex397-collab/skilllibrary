Install locations:

- repository: `.github/skills/systemd-services-codex-c/`
- user: `~/.config/github-copilot/skills/systemd-services-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Systemd Services should remain in `SKILL.md` and the references folder.
