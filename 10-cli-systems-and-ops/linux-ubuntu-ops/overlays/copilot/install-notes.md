Install locations:

- repository: `.github/skills/linux-ubuntu-ops-codex-c/`
- user: `~/.config/github-copilot/skills/linux-ubuntu-ops-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Linux Ubuntu Ops should remain in `SKILL.md` and the references folder.
