Install locations:

- repository: `.github/skills/release-binaries-codex-c/`
- user: `~/.config/github-copilot/skills/release-binaries-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Release Binaries should remain in `SKILL.md` and the references folder.
