interface:
  display_name: "Release Binaries"
  short_description: "Handle release binaries changes with preflight checks, verifi..."
  default_prompt: "Use $release-binaries-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
