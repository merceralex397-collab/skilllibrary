Install locations:

- repository: `.github/skills/config-files-xdg-codex-c/`
- user: `~/.config/github-copilot/skills/config-files-xdg-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Config Files XDG should remain in `SKILL.md` and the references folder.
