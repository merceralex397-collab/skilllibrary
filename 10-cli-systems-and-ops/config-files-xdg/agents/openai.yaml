interface:
  display_name: "Config Files and XDG"
  short_description: "Handle config files and xdg changes with preflight checks, ve..."
  default_prompt: "Use $config-files-xdg-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
