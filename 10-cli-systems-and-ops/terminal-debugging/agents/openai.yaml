interface:
  display_name: "Terminal Debugging"
  short_description: "Handle terminal debugging changes with preflight checks, veri..."
  default_prompt: "Use $terminal-debugging-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
