Install locations:

- repository: `.github/skills/terminal-debugging-codex-c/`
- user: `~/.config/github-copilot/skills/terminal-debugging-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Terminal Debugging should remain in `SKILL.md` and the references folder.
