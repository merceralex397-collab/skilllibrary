Install locations:

- repository: `.github/skills/packaging-installers-codex-c/`
- user: `~/.config/github-copilot/skills/packaging-installers-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Packaging Installers should remain in `SKILL.md` and the references folder.
