name: tui-development-codex-c
activation:
  requires_user_approval: true
  description: "Allows the agent to load the TUI Development skill and its bundled references."
permissions:
  read_paths:
    - ./SKILL.md
    - ./references/**
    - ./evals/**
  execute_paths:
    - ./scripts/**
