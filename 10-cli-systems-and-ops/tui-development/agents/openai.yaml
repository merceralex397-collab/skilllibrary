interface:
  display_name: "TUI Development"
  short_description: "Handle tui development changes with preflight checks, verific..."
  default_prompt: "Use $tui-development-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
