interface:
  display_name: "Python CLI Development"
  short_description: "Handle python cli development changes with preflight checks,..."
  default_prompt: "Use $cli-development-python-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
