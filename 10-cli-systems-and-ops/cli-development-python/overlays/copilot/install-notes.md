Install locations:

- repository: `.github/skills/cli-development-python-codex-c/`
- user: `~/.config/github-copilot/skills/cli-development-python-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for CLI Development Python should remain in `SKILL.md` and the references folder.
