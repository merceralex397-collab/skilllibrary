# Preflight Checklist

Before using Python CLI Development, confirm:

- environment and target stage,
- credentials or secrets handling,
- rollback path,
- verification command or signal,
- operator ownership.

Related skills worth checking nearby: `cli-development-go`, `bash`, `terminal-debugging`.
