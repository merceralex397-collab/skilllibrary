# Preflight Checklist

Before using Bash, confirm:

- environment and target stage,
- credentials or secrets handling,
- rollback path,
- verification command or signal,
- operator ownership.

Related skills worth checking nearby: `cli-development-go`, `cli-development-python`, `terminal-debugging`, `tui-development`.
