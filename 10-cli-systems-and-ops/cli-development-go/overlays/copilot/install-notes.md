Install locations:

- repository: `.github/skills/cli-development-go-codex-c/`
- user: `~/.config/github-copilot/skills/cli-development-go-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for CLI Development Go should remain in `SKILL.md` and the references folder.
