interface:
  display_name: "Accessibility Audit"
  short_description: "Apply accessibility audit patterns with explicit UI state, ac..."
  default_prompt: "Use $accessibility-audit-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
