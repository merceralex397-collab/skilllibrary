Install locations:

- repository: `.github/skills/accessibility-audit-codex-c/`
- user: `~/.config/github-copilot/skills/accessibility-audit-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Accessibility Audit should remain in `SKILL.md` and the references folder.
