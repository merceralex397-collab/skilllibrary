Install locations:

- repository: `.github/skills/react-native-firebase-codex-c/`
- user: `~/.config/github-copilot/skills/react-native-firebase-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for React Native Firebase should remain in `SKILL.md` and the references folder.
