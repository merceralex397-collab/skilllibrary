Install locations:

- repository: `.github/skills/analytics-instrumentation-codex-c/`
- user: `~/.config/github-copilot/skills/analytics-instrumentation-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Analytics Instrumentation should remain in `SKILL.md` and the references folder.
