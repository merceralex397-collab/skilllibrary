interface:
  display_name: "React and TypeScript"
  short_description: "Apply react and typescript patterns with explicit UI state, a..."
  default_prompt: "Use $react-typescript-codex-c to handle this task with a concrete workflow and explicit verification."
policy:
  allow_implicit_invocation: true
