Install locations:

- repository: `.github/skills/react-typescript-codex-c/`
- user: `~/.config/github-copilot/skills/react-typescript-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for React TypeScript should remain in `SKILL.md` and the references folder.
