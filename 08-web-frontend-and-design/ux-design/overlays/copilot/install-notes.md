Install locations:

- repository: `.github/skills/ux-design-codex-c/`
- user: `~/.config/github-copilot/skills/ux-design-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for UX Design should remain in `SKILL.md` and the references folder.
