Install locations:

- repository: `.github/skills/svelte-codex-c/`
- user: `~/.config/github-copilot/skills/svelte-codex-c/`

Use custom instructions only for always-on repo guidance. The portable core for Svelte should remain in `SKILL.md` and the references folder.
